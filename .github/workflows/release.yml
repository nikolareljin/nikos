name: Release

on:
  push:
    tags:
      - '[0-9]+.[0-9]+.[0-9]+'

jobs:
  release:
    uses: nikolareljin/ci-helpers/.github/workflows/release-build.yml@production
    with:
      runner: ubuntu-24.04
      artifact_paths: install.sh
      release_name: "NikOS ${{ github.ref_name }}"
