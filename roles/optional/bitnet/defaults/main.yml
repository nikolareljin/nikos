---
# Installed prefix for the BitNet CLI and the shared libraries it loads. Kept
# apart from the build tree so the command does not depend on it.
bitnet_prefix: /usr/local/lib/nikos/bitnet

bitnet_build_bin: "{{ nikos_home }}/Projects/bitnet.cpp/build/bin"
